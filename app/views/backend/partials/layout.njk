<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta content="width=device-width, initial-scale=1, maximum-scale=1, shrink-to-fit=no" name="viewport" />
        <title>{{alias}} :: TANIA IND.</title>
        <!-- General CSS Files -->
        <link rel="stylesheet" href="assets/css/app.min.css" />
        <!-- Template CSS -->
        <link rel="stylesheet" href="assets/css/style.css" />
        <link rel="stylesheet" href="assets/css/components.css" />
        <!-- Custom style CSS -->
        <link rel="stylesheet" href="assets/css/custom.css" />
        {% for csss in css %}
        <link rel="stylesheet" href="assets/{{csss}}" />
        {% endfor %}
        <link rel="shortcut icon" type="image/x-icon" href="assets/img/favicon.ico" />
    </head>

    <body>
        <div class="loader"></div>
        <div id="app">
            <div class="main-wrapper main-wrapper-1">
                <div class="navbar-bg"></div>
                {% include "./header.njk" %} {% include "./sidebar.njk" %}
                <!-- Main Content -->
                {% block content %}{% endblock %}
                <footer class="main-footer">
                    <div class="footer-left">
                        Copyright &copy; {{currYear}}
                        <div class="bullet"></div>
                        Design By <a href="javascript:;">Define Softwares Pvt. Ltd.</a>
                    </div>
                    <div class="footer-right"></div>
                </footer>
            </div>
        </div>
        <!-- General JS Scripts -->
        <script src="assets/js/app.min.js"></script>
        {% if alias=='Dashboard' %}
        <!-- JS Libraies -->
        <script src="assets/bundles/apexcharts/apexcharts.min.js"></script>
        <!-- Page Specific JS File -->
        <script src="assets/js/page/index.js"></script>
        {% endif %}
        {% for scri in script %}
        <script src="assets/{{scri}}"></script>
        {% endfor %}
        <!-- Template JS File -->
        <script src="assets/js/scripts.js"></script>
        <!-- Custom JS File -->
        <script src="assets/js/custom.js"></script>
        <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
        {% if success.length > 0 %}
            <script>swal("Great", "{{success}}", "success");</script>
        {% endif %}
        {% if error.length > 0 %}
            <script>swal("Opps!", "{{error}}", "error");</script>
        {% endif %}

    </body>
</html>
